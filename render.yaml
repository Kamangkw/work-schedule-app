services:
  - type: web
    name: work-schedule
    env: python
    rootDirectory: /
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn main:app --bind 0.0.0.0:$PORT
    plan: free
